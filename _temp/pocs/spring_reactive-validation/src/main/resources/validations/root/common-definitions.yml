$schema: http://json-schema.org/draft-07/schema#
definitions:
  hashed:
    type: string
    pattern: '^[a-f0-9]{64}$'
  tokenized:
    type: string
    pattern: '^[A-Za-z0-9-_]+$'
  encrypted:
    type: string
    pattern: '^[a-f0-9]{64}$'
